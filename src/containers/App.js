import { connect } from 'react-redux'
import AppComponent from '3dears/src/components/App'

const mapStateToProps = () => ({})

const mapDispatchToProps = () => ({})

const App = connect(mapStateToProps, mapDispatchToProps)(AppComponent)

export default App
